# 腾云香局 · Caddy 反向代理模板（一台服务器跑多个游戏）
#
# 用法：
#   1. 把本文件内容放到服务器 /etc/caddy/Caddyfile（或 include 进去）
#   2. 有域名：把 xiangju.你的域名.com 换成你的子域名，Caddy 自动签发 HTTPS 证书
#   3. 无域名（IP 直连）：用下方「IP 直连」段落，80 端口直接服务
#
# 多游戏共服：每个游戏一个 Node 进程占一个端口，这里加一个站点块即可。
# 腾云香局占 :3001；将来的游戏占 :3002、:3003……

# ---------- 路线 A：有域名（推荐，自动 HTTPS） ----------
xiangju.你的域名.com {
	reverse_proxy 127.0.0.1:3001
}

# 将来的第二个游戏示例（取消注释改域名与端口即可）：
# game2.你的域名.com {
# 	reverse_proxy 127.0.0.1:3002
# }

# ---------- 路线 B：IP 直连（无域名/未备案时用这个，注释掉路线 A） ----------
# :80 {
# 	reverse_proxy 127.0.0.1:3001
# }
